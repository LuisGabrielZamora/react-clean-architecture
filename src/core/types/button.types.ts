export enum ButtonTypeEnum {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  LINK = 'link',
  TEXT = 'text',
  GHOST = 'ghost',
  DASHED = 'dashed',
}

export type ButtonTypes =
  | 'primary'
  | 'default'
  | 'link'
  | 'text'
  | 'ghost'
  | 'dashed'
  | undefined;
